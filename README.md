# SBX Rocket — Attendance and Task Sync Fix

- Employee starts as **Not checked in** unless today has an open Firestore attendance record.
- Founder task saves from both the core data key and `sbx_wm_tasks`.
- Employee task loading supports Employee ID and employee-name assignments from older builds.

Upload all contents to the repository and hard refresh.
