(function(){
'use strict';
const KEY='sbxPayrollLiteV42',CORE='sbxRocketPremiumDataV2',ATT='sbxAttendanceV17Founder';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)], esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:0});
const uid=()=>Date.now()+Math.floor(Math.random()*1000);
let tab='dashboard',month=new Date().toISOString().slice(0,7),state=load();
function core(){try{return JSON.parse(localStorage.getItem(CORE)||'{}')}catch{return {}}}
function attendance(){try{return JSON.parse(localStorage.getItem(ATT)||'{}')}catch{return {}}}
function employees(){return (core().employees||[]).filter(e=>e.status!=='Disabled')}
function defaults(){return {settings:{payDay:7,cycle:'Monthly',workingDays:26,weeklyOffPaid:true,lateRule:'Fixed',lateDeduction:100,absentMode:'Per day',overtimeEnabled:true,overtimeMultiplier:1.5,paidLeaves:1},structures:{},runs:{},payments:[],adjustments:[]}}
function load(){try{return Object.assign(defaults(),JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{return defaults()}}
function save(){localStorage.setItem(KEY,JSON.stringify(state));window.SBXSystem?.addLog?.('Payroll updated','Payroll configuration or payment data changed','Founder','Settings','Success')}
function structure(e){let x=state.structures[e.id];if(!x){x={employeeId:e.id,basic:18000,allowance:2000,incentive:0,overtimeRate:120,lateDeduction:state.settings.lateDeduction,absentDeduction:Math.round(18000/state.settings.workingDays),otherDeduction:0,status:'Active'};state.structures[e.id]=x;save()}return x}
function attendanceStats(e){let a=attendance(),r=(a.records||{})[e.id]||{};let present=r.checkIn?1:0,late=r.late?1:0;return {present,late,absent:present?0:1,paidLeave:(a.leaves||[]).filter(l=>String(l.employeeId)===String(e.id)&&l.status==='Approved').length,overtime:0}}
function calculate(e){let s=structure(e),st=attendanceStats(e),gross=+s.basic + +s.allowance + +s.incentive,ot=state.settings.overtimeEnabled?st.overtime*+s.overtimeRate:0,late=st.late*+s.lateDeduction,abs=Math.max(0,st.absent-st.paidLeave)*+s.absentDeduction,other=+s.otherDeduction||0,bonus=(state.adjustments||[]).filter(x=>String(x.employeeId)===String(e.id)&&x.month===month&&x.type==='Bonus').reduce((a,b)=>a+Number(b.amount),0),ded=(state.adjustments||[]).filter(x=>String(x.employeeId)===String(e.id)&&x.month===month&&x.type==='Deduction').reduce((a,b)=>a+Number(b.amount),0);return {gross,ot,late,abs,other,bonus,ded,net:gross+ot+bonus-late-abs-other-ded,st}}
function title(){document.body.classList.add('payroll-page-active');$('#pageTitle').textContent='Payroll';$('#pageSubtitle').textContent='Attendance-connected salary, deductions, bonuses and payslips.';$('#moduleTitle').textContent='';$('#moduleDescription').textContent='';$('#toolbarActions').innerHTML=''}
function shell(body){return `<div class="payroll-shell"><div class="payroll-hero"><div><span class="payroll-eyebrow">TEAM OPERATIONS · PAYROLL LITE</span><h2>Payroll</h2><p>Calculate monthly salary from attendance, leave, bonus and deduction rules.</p></div><div class="payroll-actions"><input id="payrollMonth" type="month" value="${month}" style="border:1px solid #ded3e8;border-radius:12px;padding:10px"><button class="payroll-btn" id="payrollExport">Export CSV</button><button class="payroll-btn primary" id="runPayroll">Run Payroll</button></div></div><div class="payroll-tabs">${[['dashboard','Dashboard'],['monthly','Monthly Payroll'],['structures','Salary Structures'],['payslips','Payslips'],['settings','Payroll Settings']].map(([k,v])=>`<button data-payroll-tab="${k}" class="${tab===k?'active':''}">${v}</button>`).join('')}</div>${body}</div>`}
function rows(){return employees().map(e=>({e,...calculate(e),status:state.runs[month]?.[e.id]?.status||'Draft'}))}
function dashboard(){let r=rows(),total=r.reduce((a,x)=>a+x.net,0),paid=r.filter(x=>x.status==='Paid').reduce((a,x)=>a+x.net,0),ded=r.reduce((a,x)=>a+x.late+x.abs+x.other+x.ded,0),bonus=r.reduce((a,x)=>a+x.bonus+x.ot,0);return `<div class="payroll-kpis"><article class="payroll-kpi"><small>Total Payable</small><strong>${money(total)}</strong><span>${r.length} employees</span></article><article class="payroll-kpi"><small>Paid Salary</small><strong>${money(paid)}</strong><span>${r.filter(x=>x.status==='Paid').length} processed</span></article><article class="payroll-kpi"><small>Pending</small><strong>${money(total-paid)}</strong><span>Requires founder action</span></article><article class="payroll-kpi"><small>Deductions</small><strong>${money(ded)}</strong><span>Late, absence and custom</span></article><article class="payroll-kpi"><small>Bonus & Overtime</small><strong>${money(bonus)}</strong><span>Additional earnings</span></article></div><div class="payroll-grid"><article class="payroll-card"><div class="payroll-card-head"><div><h3>Current Month Payroll</h3><p>Calculated salary preview for ${month}</p></div><button class="payroll-btn" data-payroll-tab="monthly">Review Payroll</button></div>${table(r.slice(0,6))}</article><aside class="payroll-card"><div class="payroll-card-head"><div><h3>Payroll Summary</h3><p>Current processing status</p></div></div><div class="payroll-summary-box"><small>Net Salary Payable</small><strong>${money(total)}</strong><span>${state.settings.payDay}th is the default pay day</span></div><div class="payroll-mini" style="margin-top:14px"><div class="payroll-mini-row"><span>Draft</span><b>${r.filter(x=>x.status==='Draft').length}</b></div><div class="payroll-mini-row"><span>Under Review</span><b>${r.filter(x=>x.status==='Review').length}</b></div><div class="payroll-mini-row"><span>Approved</span><b>${r.filter(x=>x.status==='Approved').length}</b></div><div class="payroll-mini-row"><span>Paid</span><b>${r.filter(x=>x.status==='Paid').length}</b></div></div></aside></div>`}
function table(r){return `<div class="payroll-table-wrap"><table class="payroll-table"><thead><tr><th>Employee</th><th>Gross</th><th>Bonus/OT</th><th>Deductions</th><th>Net Salary</th><th>Status</th><th>Actions</th></tr></thead><tbody>${r.map(x=>`<tr><td><div class="payroll-person"><span class="payroll-avatar">${esc(x.e.name?.[0]||'E')}</span><div><b>${esc(x.e.name)}</b><small>${esc(x.e.role||'Employee')}</small></div></div></td><td>${money(x.gross)}</td><td>${money(x.bonus+x.ot)}</td><td>${money(x.late+x.abs+x.other+x.ded)}</td><td><b>${money(x.net)}</b></td><td><span class="payroll-badge ${x.status.toLowerCase()}">${x.status}</span></td><td><button class="payroll-btn" data-payroll-view="${x.e.id}">View</button></td></tr>`).join('')||`<tr><td colspan="7"><div class="payroll-empty">Add employees to calculate payroll.</div></td></tr>`}</tbody></table></div>`}
function monthly(){let r=rows();return `<article class="payroll-card"><div class="payroll-card-head"><div><h3>Monthly Payroll Register</h3><p>Review, approve and record payments for ${month}</p></div><button class="payroll-btn primary" id="approveAllPayroll">Approve All Drafts</button></div>${table(r)}</article>`}
function structures(){let es=employees();return `<article class="payroll-card"><div class="payroll-card-head"><div><h3>Employee Salary Structures</h3><p>Fixed salary, allowances and deduction rates</p></div></div><div class="payroll-list">${es.map(e=>{let s=structure(e);return `<div class="salary-card"><div class="payroll-person"><span class="payroll-avatar">${esc(e.name?.[0]||'E')}</span><div><b>${esc(e.name)}</b><small>${esc(e.role||'Employee')} · ${esc(e.department||'Unassigned')}</small></div></div><div><small>Basic</small><b>${money(s.basic)}</b></div><div><small>Allowance</small><b>${money(s.allowance)}</b></div><div><small>Absent/day</small><b>${money(s.absentDeduction)}</b></div><div><small>Late</small><b>${money(s.lateDeduction)}</b></div><div class="salary-actions"><button class="payroll-btn" data-salary-edit="${e.id}">Edit</button></div></div>`}).join('')||'<div class="payroll-empty">No employees available.</div>'}</div></article>`}
function payslips(){let r=rows();return `<article class="payroll-card"><div class="payroll-card-head"><div><h3>Payslips</h3><p>Generate, preview and download employee salary slips</p></div></div>${table(r)}</article>`}
function settings(){let s=state.settings;return `<article class="payroll-card"><div class="payroll-card-head"><div><h3>Payroll Rules & Settings</h3><p>Founder-controlled calculation rules</p></div><button class="payroll-btn primary" id="savePayrollSettings">Save Settings</button></div><div class="payroll-note">This Payroll Lite module uses attendance and leave data. PF, ESI, professional tax and TDS can be added later as statutory payroll.</div><div class="payroll-form-grid"><label>Salary cycle<select id="psCycle"><option ${s.cycle==='Monthly'?'selected':''}>Monthly</option><option ${s.cycle==='Bi-weekly'?'selected':''}>Bi-weekly</option></select></label><label>Default pay day<input id="psPayDay" type="number" min="1" max="28" value="${s.payDay}"></label><label>Working days/month<input id="psWorkingDays" type="number" min="1" value="${s.workingDays}"></label><label>Late deduction amount<input id="psLate" type="number" min="0" value="${s.lateDeduction}"></label><label>Paid leaves/month<input id="psPaidLeaves" type="number" min="0" value="${s.paidLeaves}"></label><label>Overtime multiplier<input id="psOTMultiplier" type="number" min="1" step=".1" value="${s.overtimeMultiplier}"></label><label>Weekly offs paid<select id="psWeeklyPaid"><option value="true" ${s.weeklyOffPaid?'selected':''}>Yes</option><option value="false" ${!s.weeklyOffPaid?'selected':''}>No</option></select></label><label>Enable overtime<select id="psOTEnabled"><option value="true" ${s.overtimeEnabled?'selected':''}>Yes</option><option value="false" ${!s.overtimeEnabled?'selected':''}>No</option></select></label><label>Absent deduction mode<select id="psAbsentMode"><option ${s.absentMode==='Per day'?'selected':''}>Per day</option><option ${s.absentMode==='No deduction'?'selected':''}>No deduction</option></select></label></div></article>`}
function render(){title();let body=tab==='dashboard'?dashboard():tab==='monthly'?monthly():tab==='structures'?structures():tab==='payslips'?payslips():settings();$('#moduleContent').innerHTML=shell(body);bind()}
function bind(){
 $$('#moduleContent [data-payroll-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.payrollTab;render()});
 $('#payrollMonth').onchange=e=>{month=e.target.value;render()};
 $('#runPayroll').onclick=()=>{state.runs[month]=state.runs[month]||{};employees().forEach(e=>state.runs[month][e.id]={status:'Review',calculatedAt:Date.now()});save();toast('Payroll calculated and moved to review');render()};
 $('#payrollExport').onclick=exportCSV;
 let aa=$('#approveAllPayroll');if(aa)aa.onclick=()=>{state.runs[month]=state.runs[month]||{};employees().forEach(e=>state.runs[month][e.id]={...(state.runs[month][e.id]||{}),status:'Approved'});save();toast('All payroll drafts approved');render()};
 $$('[data-payroll-view]').forEach(b=>b.onclick=()=>openPayslip(b.dataset.payrollView));
 $$('[data-salary-edit]').forEach(b=>b.onclick=()=>openSalary(b.dataset.salaryEdit));
 let sp=$('#savePayrollSettings');if(sp)sp.onclick=()=>{state.settings={...state.settings,cycle:$('#psCycle').value,payDay:+$('#psPayDay').value,workingDays:+$('#psWorkingDays').value,lateDeduction:+$('#psLate').value,paidLeaves:+$('#psPaidLeaves').value,overtimeMultiplier:+$('#psOTMultiplier').value,weeklyOffPaid:$('#psWeeklyPaid').value==='true',overtimeEnabled:$('#psOTEnabled').value==='true',absentMode:$('#psAbsentMode').value};save();toast('Payroll settings saved');render()}
}
function modal(titleText,html){$('#payrollModal')?.remove();let el=document.createElement('div');el.id='payrollModal';el.className='payroll-modal';el.innerHTML=`<div class="payroll-modal-card"><div class="payroll-modal-head"><div><span class="payroll-eyebrow">PAYROLL</span><h2>${esc(titleText)}</h2></div><button class="payroll-modal-close">×</button></div>${html}</div>`;document.body.appendChild(el);el.onclick=e=>{if(e.target===el||e.target.closest('.payroll-modal-close'))el.remove()};return el}
function openSalary(id){let e=employees().find(x=>String(x.id)===String(id)),s=structure(e);let m=modal('Edit Salary Structure',`<form id="salaryForm" class="payroll-form-grid"><label class="span-3">Employee<input value="${esc(e.name)}" disabled></label><label>Basic salary<input name="basic" type="number" value="${s.basic}"></label><label>Allowance<input name="allowance" type="number" value="${s.allowance}"></label><label>Incentive<input name="incentive" type="number" value="${s.incentive}"></label><label>Overtime rate/hour<input name="overtimeRate" type="number" value="${s.overtimeRate}"></label><label>Late deduction<input name="lateDeduction" type="number" value="${s.lateDeduction}"></label><label>Absent deduction/day<input name="absentDeduction" type="number" value="${s.absentDeduction}"></label><label>Other deduction<input name="otherDeduction" type="number" value="${s.otherDeduction}"></label><div class="span-3 payroll-actions" style="justify-content:flex-end"><button class="payroll-btn primary">Save Structure</button></div></form>`);m.querySelector('#salaryForm').onsubmit=e2=>{e2.preventDefault();let f=new FormData(e2.target);['basic','allowance','incentive','overtimeRate','lateDeduction','absentDeduction','otherDeduction'].forEach(k=>s[k]=Number(f.get(k)||0));save();m.remove();toast('Salary structure saved');render()}}
function openPayslip(id){let e=employees().find(x=>String(x.id)===String(id)),x=calculate(e),status=state.runs[month]?.[id]?.status||'Draft';let m=modal('Payslip Preview',`<div class="payslip"><div class="payslip-head"><div><h3>SBXROCKET</h3><p>Salary slip · ${month}</p></div><div><b>${esc(e.name)}</b><p>${esc(e.role||'Employee')}</p></div></div><div class="payslip-grid"><div><h4>Earnings</h4><div class="payslip-lines"><div class="payslip-line"><span>Basic Salary</span><b>${money(structure(e).basic)}</b></div><div class="payslip-line"><span>Allowance</span><b>${money(structure(e).allowance)}</b></div><div class="payslip-line"><span>Incentive</span><b>${money(structure(e).incentive)}</b></div><div class="payslip-line"><span>Bonus / Overtime</span><b>${money(x.bonus+x.ot)}</b></div></div></div><div><h4>Deductions</h4><div class="payslip-lines"><div class="payslip-line"><span>Late</span><b>${money(x.late)}</b></div><div class="payslip-line"><span>Absence</span><b>${money(x.abs)}</b></div><div class="payslip-line"><span>Other</span><b>${money(x.other+x.ded)}</b></div><div class="payslip-line payslip-total"><span>Net Salary</span><b>${money(x.net)}</b></div></div></div></div><div class="payroll-actions" style="justify-content:flex-end;margin-top:22px"><select id="payrollStatus" class="payroll-btn"><option ${status==='Draft'?'selected':''}>Draft</option><option ${status==='Review'?'selected':''}>Review</option><option ${status==='Approved'?'selected':''}>Approved</option><option ${status==='Paid'?'selected':''}>Paid</option><option ${status==='Hold'?'selected':''}>Hold</option></select><button class="payroll-btn" id="printPayslip">Print</button><button class="payroll-btn primary" id="savePayrollStatus">Save Status</button></div></div>`);m.querySelector('#savePayrollStatus').onclick=()=>{state.runs[month]=state.runs[month]||{};state.runs[month][id]={...(state.runs[month][id]||{}),status:m.querySelector('#payrollStatus').value,updatedAt:Date.now()};save();m.remove();toast('Payroll status updated');render()};m.querySelector('#printPayslip').onclick=()=>window.print()}
function exportCSV(){let r=rows(),csv=[['Employee','Month','Gross','Bonus & OT','Deductions','Net Salary','Status'],...r.map(x=>[x.e.name,month,x.gross,x.bonus+x.ot,x.late+x.abs+x.other+x.ded,x.net,x.status])].map(row=>row.map(v=>'"'+String(v).replace(/"/g,'""')+'"').join(',')).join('\n');let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`payroll-${month}.csv`;a.click();URL.revokeObjectURL(a.href);toast('Payroll CSV exported')}
document.addEventListener('click',e=>{let p=e.target.closest('.payroll-nav');if(p){setTimeout(render,20);return}let n=e.target.closest('.nav-item');if(n&&!n.classList.contains('payroll-nav'))document.body.classList.remove('payroll-page-active')},true);
window.SBXPayroll={render};
})();
